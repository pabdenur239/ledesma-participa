<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Necesario en release (no solo debug): la app consume la API
         pública de solo lectura de ledesmaparticipa.com.ar. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Requerido desde Android 13 (API 33) para poder mostrar
         notificaciones — sin esto, FirebaseMessaging.requestPermission()
         no tiene nada que pedir. Notificaciones push: ver README.md. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="Ledesma Participa"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- FCM: canal/ícono por defecto para las notificaciones que llegan
             con la app en segundo plano o cerrada (Android las muestra él
             solo en ese caso, sin código Dart — con la app en primer plano
             se muestran a mano, ver notification_service.dart). Sin esto,
             Firebase no sabe qué canal/ícono usar y la notificación no
             aparece. El id del canal debe coincidir exactamente con
             CANAL_NOTIFICACIONES_ID en notification_service.dart. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="ledesma_participa_importantes" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
